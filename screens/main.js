import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const main = ({nagivation}) => {
  return (
    <View>
      <Text>MAIN</Text>
    </View>
  );
};

export default main;

const styles = StyleSheet.create({});
